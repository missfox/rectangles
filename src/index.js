import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './containers/app/app';

const reactAppSelector = document.querySelector('.react-app');
ReactDOM.render(<App/>, reactAppSelector);
